import { ArrowDownRight,ArrowUpRight } from "lucide-react";
import { CountUp } from "./count-up";
export function MetricCard({label,value,note,attention=false}:{label:string;value:string|number;note:string;attention?:boolean}){const percent=typeof value==="string"&&value.endsWith("%");const numeric=typeof value==="number"?value:percent?Number.parseFloat(value):null;return <div className={`border p-5 ${attention?"border-amber-300 bg-amber-50/50":"bg-card"}`}><span className="eyebrow">{label}</span><strong className="number-tabular mt-4 block text-3xl font-bold tracking-[-.04em]">{numeric===null?value:<CountUp value={numeric} suffix={percent?"%":""}/>}</strong><span className="mt-2 flex items-center gap-1 text-xs text-muted-foreground">{attention?<ArrowUpRight className="size-3 text-amber-700"/>:<ArrowDownRight className="size-3 text-civic"/>}{note}</span></div>}
