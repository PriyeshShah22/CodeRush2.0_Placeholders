"use client";
import { useState } from "react";import { toast } from "sonner";import { api } from "@/lib/api";import { Button } from "@/components/ui/button";import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog";import { Label } from "@/components/ui/label";import { Textarea } from "@/components/ui/textarea";
export function CorrectionButton({complaintId}:{complaintId:string}){const [message,setMessage]=useState("");const [open,setOpen]=useState(false);async function submit(){try{await api(`/complaints/${complaintId}/appeals`,{method:"POST",body:JSON.stringify({kind:"correction",message})});toast.success("Human review request recorded");setOpen(false)}catch(e){toast.error(e instanceof Error?e.message:"Could not submit request")}}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="link" className="mt-1 h-auto p-0">Request a correction</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>What should we review?</DialogTitle><DialogDescription>This will reopen the complaint for human review. It will not delete the original report.</DialogDescription></DialogHeader><div><Label htmlFor="correction">Correction or concern</Label><Textarea id="correction" value={message} onChange={e=>setMessage(e.target.value)} className="mt-2" placeholder="Describe what appears incorrect…"/></div><DialogFooter><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={submit} disabled={message.trim().length<12}>Request review</Button></DialogFooter></DialogContent></Dialog>}

